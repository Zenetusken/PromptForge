# PromptForge Environment Configuration
# Copy to .env and uncomment/modify as needed.
# Values shown are the defaults from backend/app/config.py.

# Server
FRONTEND_URL=http://localhost:5199
BACKEND_PORT=8000
MCP_PORT=8001
# HOST=0.0.0.0
# MCP_HOST=127.0.0.1
# BACKEND_HOST=127.0.0.1    # set to 'backend' in Docker (MCP→backend webhook)

# Database (absolute path recommended for non-default locations)
# DATABASE_URL=sqlite+aiosqlite:///./data/promptforge.db

# --- Security ---
# Bearer token for API authentication (empty = disabled, backward compatible)
# AUTH_TOKEN=
# Bearer token for MCP server authentication (empty = disabled)
# MCP_AUTH_TOKEN=
# Shared secret for internal webhook between MCP server and backend
# (auto-generated if empty)
# INTERNAL_WEBHOOK_SECRET=
# Symmetric key for encrypting OAuth tokens at rest (auto-generated on first use if empty)
# ENCRYPTION_KEY=
# Rate limits (requests per minute)
# RATE_LIMIT_RPM=60
# RATE_LIMIT_OPTIMIZE_RPM=10

# --- GitHub OAuth (Workspace Hub) ---
# Register a GitHub OAuth App: https://github.com/settings/developers
# GITHUB_CLIENT_ID=
# GITHUB_CLIENT_SECRET=
# GITHUB_REDIRECT_URI=http://localhost:8000/api/github/callback

# --- LLM Provider Configuration ---
# Leave LLM_PROVIDER empty for auto-detection (recommended).
# Auto-detect order: Claude CLI → ANTHROPIC_API_KEY → OPENAI_API_KEY → GEMINI_API_KEY
# LLM_PROVIDER=               # explicit: claude-cli, anthropic, openai, gemini

# Claude CLI (default, zero-config with MAX subscription)
# CLAUDE_MODEL=claude-opus-4-6

# Anthropic API (direct API access, requires key)
# ANTHROPIC_API_KEY=
# CLAUDE_MODEL=claude-opus-4-6   # shared with Claude CLI

# OpenAI (requires key, install: pip install promptforge-backend[openai])
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4.1

# Google Gemini (requires key, install: pip install promptforge-backend[gemini])
# GEMINI_API_KEY=
# GEMINI_MODEL=gemini-2.5-pro

# --- Docker ---
# When running via docker compose, these are set automatically in docker-compose.yml.
# Override them in .env only if you need non-default values:
#   FRONTEND_URL      - set if accessing from a non-localhost hostname (CORS)
#   GITHUB_REDIRECT_URI - set if your backend is behind a reverse proxy
#   AUTH_TOKEN         - API authentication (NOT baked into the frontend image;
#                        for Docker auth, use a reverse proxy)
